[package]
name = "tui-stream-drt"
version = "0.1.0"
edition = "2021"

# The Rust mirror of lean/TuiStreamSpec.lean (the `lean-verify` op=drt
# production executable). Std-only: the differential gate must stay
# buildable and auditable without dependencies.
